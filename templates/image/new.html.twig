{% extends 'base_back_3.html.twig' %}

{% block title %}New Vehicle{% endblock %}

{% block body %}
    <h1>Create new Vehicle</h1>

    <div class="p-2">
    {{ form_start(form) }}
    {{ form_widget(form) }}
        <button class="btn btn-success mb-2">{{ button_label|default('Guardar') }}</button>
    {{ form_end(form) }}


    <button class="btn btn-primary">
        <a href="{{ path('app_vehicle_index') }}" class="text-light text-decoration-none">Tornar a la llista</a>
    </button>
    </div>
{% endblock %}
