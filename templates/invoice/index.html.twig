{% extends 'base_back_2.html.twig' %}

{% block title %}Invoice index{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('equip3/css/llistaInvoices.css') }}">
{% endblock %}

{% block body %}
        <section class="">

    <div class="container-fluid">
        <div class="row">
            <div class="col-1"></div>
            <div class="col-10">
                <h1 class="text-center">Llista de Factures</h1>
                <div {{ vue_component('InvoiceUpdateModal') }}></div>
                <div id="div-table"{{ vue_component('InvoiceList_component', {'invoices': paginatedInvoices.items})}} >
                </div>
            </div>
            <div class="col-1"></div>
    </div>
    <div class="navigation d-flex justify-content-center mt-3" >
        {{ knp_pagination_render(paginatedInvoices) }}
    </div>
{% endblock %}