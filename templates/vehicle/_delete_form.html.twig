<div class="d-flex mt-3 mb-3">
    <form method="post" id="sweetAlertDelete" action="{{ path('app_vehicle_delete', {'id': vehicle.id}) }}">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehicle.id) }}">
        <button type="button" class="btn btn-danger">Esborrar</button>
    </form>
</div>