{% extends 'base_back_3.html.twig' %}

{% block title %}Edit Vehicle{% endblock %}

{% block body %}
    <h1>Edit Vehicle</h1>

    {{ include('vehicle/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="d-flex mt-3 mb-3">
        <a href="{{ path('app_vehicle_index') }}"
           class="link-underline link-underline-opacity-0 btn btn-secondary text-light">back to list</a>
    </div>

    {{ include('vehicle/_delete_form.html.twig') }}
{% endblock %}
